# deadbolt-randomiser
Not going to publish the actual code in this repo, decompile it with [UMT](https://github.com/UnderminersTeam/UndertaleModTool) if necessary.

## How to use
Either:

A) Rename to data.win and replace the vanilla file in the game's directory.

B) Place in the game's directory, add "-game totalrando.win" as a launch argument.

## TODO:
* Add seed lock
* Fix weird Ibzan interactions
* Linux version?
